{
  "name": "esdb-schema",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic ESDB ORM IR v1 compiler: validate, canonicalize, hash, package migrations, and generate C++/ES3/TS bindings.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22.18.0"
  },
  "bin": {
    "esdb-schema": "./bin/esdb-schema.mjs"
  },
  "scripts": {
    "test": "node ../../tests/orm/run.mjs",
    "check": "node ./bin/esdb-schema.mjs --help"
  }
}
